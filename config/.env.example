# Exchange API (optional for paper trading)
EXCHANGE_API_KEY=your_api_key_here
EXCHANGE_API_SECRET=your_api_secret_here
# Required when API credentials are provided (used for encrypted local vault persistence)
CONFIG_MASTER_KEY=replace_with_a_long_random_secret

# Database
DATABASE_PATH=data/database/trading.db

# Logging
LOG_LEVEL=INFO
