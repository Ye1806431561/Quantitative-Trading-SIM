[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    error::DeprecationWarning
    error::ResourceWarning
    error::pytest.PytestUnraisableExceptionWarning
markers =
    unit: 单元测试（默认标记）
    integration: 集成测试（关键路径联调）
