# @defaultSensitive=true
# @defaultRequired=infer

# 核心运行参数
# @type=enum(DEBUG,INFO,WARNING,ERROR,CRITICAL) @sensitive=false
LOG_LEVEL=INFO

# @type=string @sensitive=false
DATABASE_PATH=data/database/trading.db

# 交易所凭证（敏感）
# @type=string @required @sensitive
EXCHANGE_API_KEY=

# @type=string @required @sensitive
EXCHANGE_API_SECRET=

# 本地凭证加密主密钥（敏感）
# @type=string(min=16) @required @sensitive
CONFIG_MASTER_KEY=
